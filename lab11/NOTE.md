**Lab 11:** Create a Secret and mount as file:

Run:
```bash
kubectl create secret generic db-secret --from-literal=PASSWORD=supersecret

```

👉 Goal: Be fluent in using ConfigMap and Secret references inside Deployments.

